# Dev-only dependencies for running the test suite locally / in CI.
# Kept separate from requirements.txt, which stays stdlib-only for the
# production fetch/build scripts (see the comment there).
pytest
